<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="csrf-token" content="<%= form_authenticity_token %>">
  <title>Workshop — Repair Tracker</title>
  <link rel="stylesheet" href="<%= asset_path 'shared.css' %>">
  <link rel="stylesheet" href="<%= asset_path 'workshop.css' %>">
</head>
<body>

  <!-- TOP BAR -->
  <div class="topbar">
    <span>Repair Tracker — Workshop</span>
    <button onclick="logout()">Logout</button>
  </div>

  <div class="container">
    <h2>Requests</h2>
    <div class="message" id="page-message"></div>
    <div id="requests-area">
      <p class="empty">Loading...</p>
    </div>
  </div>

  <script src="<%= asset_path 'workshop.js' %>"></script>

</body>
</html>